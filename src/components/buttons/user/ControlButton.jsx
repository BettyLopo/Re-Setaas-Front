import React from 'react'

const ControlButton = ({className}) => {
  return (
    <div className={`border-[0.18rem] border-brown shadow-bigs rounded-cont h-[7.875rem] ${className}`}>
    </div>
  )
}

export default ControlButton
